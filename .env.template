CANVA_FRONTEND_PORT=8080
CANVA_BACKEND_PORT=3001
CANVA_BACKEND_HOST=https://api-canva.opendex.dev/dist
CANVA_APP_ID=# TODO: Add your app's ID here to configure your backend for JWT verification
CANVA_APP_ORIGIN=# TODO: Add your app's origin here from the "Developer Portal -> Settings -> Security" to enable HMR
CANVA_HMR_ENABLED=FALSE # TODO: set to TRUE to enable HMR

# Cloudflare R2 credentials for icon uploads
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PREFIX=

# Public CDN base URL where SVGs are hosted (e.g., https://pub-xxxx.r2.dev/icons)
ICON_CDN_BASE_URL=

# Optional: override when deploying on Vercel as Development URL
# (Vercel UI will inject these values during build)
VERCEL="true"
